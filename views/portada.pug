#portada
  #telon
    include lenguajes
    #titulo
      h1=titulo
    #flechita
    #logo
  video(autoplay=true loop=true muted=true class=celu ? 'mobile' : 'desktop')
    source(src="/video/portada-web-h264.mp4" type="video/mp4")
