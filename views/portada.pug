#portada(class=celu ? 'mobile' : 'desktop')
  #telon
    include lenguajes
    #titulo
      h1=titulo
    #flechita
    #logo
  if !celu
  video(autoplay=true playsinline=true loop=true muted=true )
    source(src="/video/portada-web-h264.mp4" type="video/mp4")
